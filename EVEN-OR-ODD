number = int(input("Enter a number: ")) #Informs the user to print a number
if number % 2 == 0: #Checks the users value if it is even
  print(f"{number} is even.") #If Value is even, informs the user
else: 
  print(f"{number} is odd.") #If Value is Not even, informs the user
